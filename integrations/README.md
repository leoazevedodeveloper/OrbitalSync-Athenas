# Integrações (artefatos)

- **`n8n/`** — Spotify, Google Calendar; ver `n8n/README.md`.

Webhooks n8n: hooks em **`config/webhooks.json`** (ou Supabase `athena_webhooks`).

Geração de imagem: **Nano Banana 2** via Gemini API (free tier 500/dia). Imagens salvas em `data/generated-images/`.
